# In-memory catalog for smoke testing the image. Real catalogs (iceberg
# pointing at Lakekeeper, hive pointing at refined parquet) are added by
# the platform that consumes this image, not baked into the runtime.
connector.name=memory
