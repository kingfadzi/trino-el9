node.environment=development
# node.id is auto-generated at first start and persisted under node.data-dir.
# Don't pin it -- a fixed UUID across nodes breaks discovery.
node.data-dir=/var/lib/trino/data
